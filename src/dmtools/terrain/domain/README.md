# Terrain domain

This package will contain dependency-light terrain concepts and invariants:
units, coordinates, constraints, profiles, grids, manifests, stage identifiers,
and errors.

Domain code must not read files, call external tools, render images, or depend on
CLI and web frameworks.
